function Count({ count }) {
  return (
    <h2
      style={{
        textAlign: "center",
        fontWeight: "800",
        fontSize: "45px",
        color: "#fefefe",
      }}
    >
      {count}
    </h2>
  );
}

export default Count;
